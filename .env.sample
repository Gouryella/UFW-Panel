# Frontend gateway configuration
# Copy this file to .env (or export the variables manually) before running

# Password required when posting to /api/auth
AUTH_PASSWORD=changeme

# HMAC secret used to sign the session cookie (must be kept private)
JWT_SECRET=replace-this-with-a-long-random-string

# Optional token lifetime; accepts values like 1d, 12h, 30m, or seconds
JWT_EXPIRATION=1d

# TCP port for the Go server to listen on (defaults to 8080 if omitted)
PORT=8080

# Location of the exported Next.js assets served by the Go gateway
FRONTEND_DIST_DIR=./out

# Path to the SQLite database that stores backend connection details
FRONTEND_DB_PATH=./database/ufw-webui.db

# Optional comma-separated list of allowed origins for browser clients.
# Leave unset to allow all origins.
# FRONTEND_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# When running `yarn dev`, set this so the browser calls the Go API server
NEXT_PUBLIC_API_BASE=http://localhost:8080
